# Backend Environment Variables
# Copy this to backend/.env for local development
# On Railway: set these in the Railway dashboard under Environment Variables

# No secrets needed for basic mode (Binance public API + yfinance are free/public)
# Optional: add API keys here if you use a paid news API in the future

# CORS — set this to your Vercel frontend URL in production
# ALLOWED_ORIGIN=https://bear-bull-predection1.vercel.app/
